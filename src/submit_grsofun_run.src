#!/bin/bash

# requires large memory, finally set to 200 GB

njobs=100
for ((n=1;n<=${njobs};n++)); do
    echo "Submitting chunk number $n ..."
    bsub -W 72:00 -u bestocke -J "jobname $n" -R "rusage[mem=200000]" "Rscript --vanilla ../analysis/grsofun_run.R $n $njobs"
done